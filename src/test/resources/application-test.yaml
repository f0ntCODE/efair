#for tests

spring:
  datasource:
    url: jdbc:h2:mem:testCaseDb
    driver-class-name: org.h2.Driver
    username: sa
    password:

    #JPA
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update